spring:
  application:
    name: gateway
  cloud:
    config:
      uri: http://configserver:8888
      username: my-username
      password: my-password
      failFast: true
#encrypt:
#  failOnError: false